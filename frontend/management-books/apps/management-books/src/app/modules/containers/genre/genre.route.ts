import { Route } from '@angular/router';
import { GenreListComponent } from './containers/genre-list/genre-list.component';

export const genreRoute: Route[] = [{ path: '', component: GenreListComponent }];
