import { Route } from '@angular/router';
import { PublisherListComponent } from './containers/publisher-list/publisher-list.component';

export const publisherRoute: Route[] = [{ path: '', component: PublisherListComponent }];
