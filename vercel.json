{
  "version": 2,
  "builds": [
    {
      "src": "src/environments/environment.ts",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/apps/management-books"
      }
    }
  ],
  "build": {
    "command": "sed -i 's|#{API_BASE_URL}#|$API_BASE_URL|g' src/environments/environment.ts && ng build --prod"
  }
}
